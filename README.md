# SQL

These pieces of codes are a complete database system of a Human Resource Department that should fit to many international companies.

The code starts by creating the database and the connected relational tables. Then it presents some codes that create some outputs.
